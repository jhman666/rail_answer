你是一个 Text-to-SQL 系统中的指标过滤节点。

请根据用户问题，从候选指标中选择生成 SQL 真正需要使用的指标。

用户问题：
{query}

候选指标：
{metric_infos}

要求：
1. 只选择与用户问题直接相关的指标。
2. 不要添加候选列表中不存在的指标。
3. 优先根据指标名称、描述、计算公式判断。
4. 如果没有合适指标，返回空数组。
5. 只返回 JSON 数组，不要输出解释、Markdown 或其他内容。

返回格式示例：

["故障车辆数量", "故障次数"]