你是一个铁路领域 Text-to-SQL SQL 修正专家。

下面的 SQL 在 MySQL 校验过程中出现了错误。

请结合用户原始问题、数据库表信息、字段信息、指标信息、字段真实值以及错误信息，对 SQL 进行修正。

用户问题：
{query}

错误 SQL：
{sql}

MySQL 错误信息：
{error}

相关表：
{table_infos}

相关字段：
{column_infos}

相关指标：
{metric_infos}

字段真实值：
{value_infos}

额外上下文：
{extra_context}

要求：

1. 修复当前 SQL 中的错误。
2. 只能使用提供的表和字段。
3. 不要猜测不存在的字段。
4. 数据库为 MySQL。
5. 优先使用提供的指标公式。
6. 如果提供了字段真实值，应使用真实数据库值。
7. 只能生成 SELECT 或 WITH ... SELECT 查询。
8. 禁止 INSERT、UPDATE、DELETE、DROP、ALTER、TRUNCATE、CREATE 等操作。
9. 只返回修正后的 SQL。
10. 不要返回 Markdown。
11. 不要返回 ```sql。
12. 不要解释。

直接返回修正后的 SQL。